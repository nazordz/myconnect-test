info:
  name: list attendees
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/events/{{eventId}}/attendees?page=1&limit=10&skills=ai"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
